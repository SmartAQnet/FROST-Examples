# install

Install python3 (www.python.org) and make sure that python is in your PATH environment variable.

Then, for Linux run 
```
install-linux.sh
```

For Windows run
```
install-windows.bat
```

Downloading and installing can take several minutes.
Afterwards, for Linux run
```
run-linux.sh
```

For Windows run
```
run-windows.bat
```

This will start Jupyter. Navigate to the "src" folder and open a specific ipython notebook.

# convert to CSV

Open Converter.ipynb and change "ThingID", "starttime" and "endtime" to your needs. Then run the whole notebook. The CSV files can be found inside a newly created folder corresponding to the ThingID.

